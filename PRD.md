# MD来了本地版产品需求文档（MVP）

> 文档状态：Draft v0.1  
> 产品形态：Windows/macOS 桌面客户端  
> 核心原则：本地优先、打开即写、所见即所得式双栏预览

## 1. 产品概述

MD来了是一款面向个人写作者、产品经理和开发者的本地 Markdown 文档编辑器。首版复原参考图中的核心布局和编辑体验，但不依赖账户、服务器或在线服务。

用户可以选择本地文件夹作为工作区，在应用内浏览和管理 Markdown 文件，以源码与实时预览双栏方式编辑，并使用大纲、全文搜索、字数统计等辅助功能完成日常写作。

### 1.1 产品目标

- 让用户在 10 秒内打开本地工作区并开始写作。
- 提供稳定、流畅的 Markdown 编辑与实时预览体验。
- 所有文档默认保存在用户选择的本地目录中，格式保持开放和可迁移。
- 建立可扩展的桌面架构，为后续备份、分享、同步和版本管理预留接口。

### 1.2 非目标（首版不做）

- 用户注册、登录和会员体系。
- 云同步、多人协作、评论和在线状态。
- 分享链接、公开发布与访问权限控制。
- 自动备份、跨设备恢复和历史版本。
- DOCX/PDF 高保真导出。
- 插件市场、移动端和 Web 端。

## 2. 目标用户与使用场景

### 2.1 目标用户

- 产品经理：撰写 PRD、会议纪要和用户故事。
- 开发者：维护项目文档、接口说明和知识笔记。
- 内容写作者：撰写文章、教程和个人知识库。

### 2.2 核心场景

1. 用户首次启动应用，选择一个本地文件夹作为工作区。
2. 用户在左侧文件树中新建或打开 `.md` 文件。
3. 用户在编辑区输入 Markdown，并在右侧即时查看渲染结果。
4. 用户通过大纲快速跳转到某个标题。
5. 用户搜索工作区中的文件名或文档内容。
6. 应用自动保存修改；用户关闭并再次启动后回到上次工作状态。

## 3. 产品范围与优先级

| 模块 | 功能 | 优先级 | MVP |
| --- | --- | --- | --- |
| 工作区 | 打开本地文件夹、最近工作区 | P0 | 是 |
| 文件管理 | 文件树、新建、重命名、删除、移动 | P0 | 是 |
| 编辑器 | Markdown 语法高亮、行号、撤销/重做 | P0 | 是 |
| 预览 | 实时渲染、代码高亮、表格、任务列表 | P0 | 是 |
| 保存 | 自动保存、手动保存、未保存状态提示 | P0 | 是 |
| 大纲 | 标题树、点击跳转、跟随光标高亮 | P0 | 是 |
| 搜索 | 文件名与工作区全文搜索 | P1 | 是 |
| 统计 | 字数、字符数、行数、预计阅读时间 | P1 | 是 |
| 显示模式 | 双栏、仅编辑、仅预览 | P1 | 是 |
| 主题 | 浅色、深色、跟随系统 | P1 | 是 |
| 导出 | 导出为 HTML | P1 | 是 |
| 快捷键 | 常用编辑与文件操作快捷键 | P1 | 是 |
| 标签页 | 同时打开多个文件 | P2 | 可延后至 1.1 |
| 图片管理 | 粘贴图片并复制到本地 assets | P2 | 可延后至 1.1 |

## 4. 信息架构与页面布局

应用采用四区布局，并允许折叠侧栏：

1. 顶部应用栏：工作区名称、当前文件名、保存状态、显示模式、主题和设置。
2. 左侧导航栏：新建文件、快速入口、工作区文件树、存储位置提示。
3. 主工作区：Markdown 编辑器与实时预览，默认各占 50%。
4. 右侧信息栏：大纲、文档统计；后续可承载评论、历史版本等能力。

与参考图相比，首版删除分享、协作者头像、评论、历史版本和云存储容量等在线元素。

## 5. 详细功能需求

### 5.1 工作区

- 首次启动展示欢迎页，提供“打开文件夹”和“打开 Markdown 文件”。
- 用户授权后读取所选目录及其子目录。
- 记录最多 10 个最近工作区，只保存路径和最后打开时间。
- 工作区无效或目录被移动时给出明确提示，并允许重新选择。
- 默认忽略 `.git`、`node_modules`、系统隐藏目录，可在设置中调整。

### 5.2 文件树与文件操作

- 展示目录及 `.md`、`.markdown` 文件，目录可展开或折叠。
- 支持新建文件、新建文件夹、重命名、移动到回收站。
- 新建文件自动补全 `.md` 扩展名。
- 删除前二次确认；首版不提供应用内回收站。
- 监听外部文件变化：未编辑时自动刷新；存在本地修改时提示用户选择重新加载或保留编辑内容。

### 5.3 Markdown 编辑器

- 支持 CommonMark 与 GitHub Flavored Markdown 常用语法。
- 支持标题、粗体、斜体、删除线、列表、任务列表、引用、链接、图片、表格、行内代码和代码块。
- 提供行号、当前行高亮、括号匹配、查找替换、撤销重做。
- 工具栏按钮插入常用语法，不强制用户记忆 Markdown 标记。
- 默认启用软换行；Tab 宽度可配置为 2 或 4 个空格。
- 支持常用快捷键，如 `Ctrl/Cmd+S`、`Ctrl/Cmd+F`、`Ctrl/Cmd+B`、`Ctrl/Cmd+I`。

### 5.4 保存机制

- 文本变化后 800 ms 无新输入时自动保存。
- 顶部展示“保存中”“已保存”“保存失败”三种状态。
- `Ctrl/Cmd+S` 立即保存。
- 写入使用临时文件加原子替换，降低意外中断造成文件损坏的风险。
- 关闭窗口前若仍有未落盘内容，必须尝试保存；失败时阻止关闭并提示。

### 5.5 实时预览

- 输入后 150 ms 内启动增量防抖渲染。
- 支持 GFM 表格、任务列表、自动链接和代码语法高亮。
- 默认禁止执行文档中的脚本和不安全 HTML。
- 支持编辑区与预览区按标题/段落近似同步滚动；可在设置中关闭。
- 点击预览中的站外链接时使用系统默认浏览器打开。

### 5.6 大纲

- 从 H1–H6 标题生成层级大纲。
- 点击标题，将编辑器定位到对应行。
- 编辑器光标移动时，高亮当前所属标题。
- 文档没有标题时显示空状态说明。

### 5.7 搜索

- 顶部搜索框可搜索文件名和 Markdown 正文。
- 搜索结果显示文件名、相对路径和命中片段。
- 点击结果打开文件并定位到命中位置。
- 首版限制在当前工作区；忽略二进制文件和被排除目录。

### 5.8 文档统计

- 实时显示字数、字符数和行数。
- 阅读时间按每分钟 300 个中文字符或 200 个英文单词估算。
- 统计区域位于右侧栏底部，可折叠。

### 5.9 显示与设置

- 三种模式：双栏、仅编辑、仅预览。
- 主题：浅色、深色、跟随系统。
- 可配置字体大小、编辑器字体、Tab 宽度、自动保存间隔和同步滚动。
- 设置保存在应用本地配置目录，不写入用户文档目录。

### 5.10 HTML 导出

- 将当前 Markdown 导出为独立 HTML 文件。
- 默认内嵌基础排版样式和代码高亮样式。
- 导出前由用户选择目标路径；不覆盖源 Markdown。

## 6. 关键交互规则

- 单击文件：在当前编辑区域打开。
- 右键文件或目录：显示新建、重命名、删除、在资源管理器/Finder 中显示。
- 拖动中间分隔条：调整编辑与预览宽度；记录用户上次比例。
- 点击大纲项：编辑器滚动并短暂高亮目标标题。
- 外部修改冲突：显示非阻塞横幅，提供“重新加载”“覆盖磁盘文件”“另存为”。
- 保存失败：状态标红并展示原因，保留内存中的编辑内容。

## 7. 异常与空状态

- 未打开工作区：展示欢迎页和最近工作区。
- 工作区为空：提示新建 Markdown 文件。
- 文件无读取权限：展示原因，不让编辑区进入可编辑状态。
- 文件编码异常：首版仅正式支持 UTF-8，检测失败时提示转换或取消。
- 文件被外部删除：保留当前内容，并允许另存为新文件。
- 超大文件：超过 5 MB 时关闭实时预览并提示用户手动启用。

## 8. 非功能需求

### 8.1 性能

- 冷启动目标：主流近五年电脑上不超过 2 秒。
- 普通文档（1 MB 以内）按键到编辑区显示低于 50 ms。
- 普通文档预览刷新感知延迟低于 200 ms。
- 含 5,000 个 Markdown 文件的工作区应在 3 秒内显示首屏文件树。

### 8.2 稳定性与数据安全

- 应用崩溃不应破坏原始 Markdown 文件。
- 文件写入失败必须可见，不能静默丢失内容。
- 不上传文件名、路径、正文或使用统计。
- 所有网络访问默认关闭；首版不集成遥测。

### 8.3 可用性与兼容性

- 首发支持 Windows 10/11，架构保留 macOS 支持。
- 支持 125%–200% 系统缩放。
- 主要操作可通过键盘完成，交互控件具备清晰焦点状态。

## 9. MVP 验收标准

满足以下条件即可视为首版可交付：

1. 用户可以打开任意本地目录并浏览其中的 Markdown 文件。
2. 用户可以新建、打开、编辑、重命名和删除 Markdown 文件。
3. Markdown 编辑内容能够自动保存，重启应用后内容不丢失。
4. 编辑区支持常用 Markdown 语法高亮，预览区正确渲染 GFM 内容。
5. 大纲能够展示标题层级并跳转到对应位置。
6. 搜索能够返回当前工作区的文件名和正文命中结果。
7. 双栏、仅编辑、仅预览三种模式可切换。
8. 字数、字符数、行数和阅读时间统计正确更新。
9. 浅色与深色主题均不存在明显布局或对比度问题。
10. 断网状态下所有首版功能均可正常使用。

## 10. 后续版本候选

- v1.1：多标签页、图片粘贴与本地资源管理、PDF 导出、恢复未保存草稿。
- v1.2：本地快照、Git 集成、定时备份和版本对比。
- v2.0：可选账户、端到端加密同步、分享链接和多人协作。
